import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Fish = () => {
  return (
    <View>
      <Text>Fish</Text>
    </View>
  );
};

export default Fish;

const styles = StyleSheet.create({});
